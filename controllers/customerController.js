const _ = require('lodash');
const { User } = require('../models/User');

const updateFuelAllocation = (fuelType, fuelAllocation) => {
    
    return fuelAllocation;
}

module.exports = {updateFuelAllocation}